const WelcomeText = () => {

    // 1) a component is a function that returns html code no matter how basic

    // 2) to use a component you have to first export it

    // 3) import it in the file you are going to use it in

    // 4) just like writing a html element, you write the name of your component in between <> with respect to capital letters


    return(
        <div>
            Welcome to our webpage!!!
        </div>
    )
}

export default WelcomeText